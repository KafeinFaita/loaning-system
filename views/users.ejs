<!DOCTYPE html>
<html lang="en">
    <%- include('./partials/head.ejs') %>
<body>
    <h1>USERS</h1>

    <% if (names.length > 0) { %>
        <% names.forEach(name => { %>
            <p><%- name %></p>
        <% }) %> 
    <% } %>
</body>
</html>