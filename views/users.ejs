<!DOCTYPE html>
<html lang="en">
    <%- include('./partials/head.ejs') %>
<body>
    <%- include('./partials/navbar.ejs') %>
    <div class="container">
        <%- include('./partials/sidebar.ejs') %>
        <main>
            <h1>List of Users</h1>
            <% if (names.length > 0) { %>
                <% names.forEach(name => { %>
                    <p><%- name %></p>
                <% }) %> 
            <% } %>
        </main>
    </div>
</body>
</html>